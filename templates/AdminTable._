<table>
    <thead><tr>
    <% if (header) { _.forEach(header, function(v) {%>
    <th <% if (v.className) { %>class='<%= v.className %>'<% } %>><%= v.title %></th>
    <% }); } %>
    </tr></thead>
</table>
